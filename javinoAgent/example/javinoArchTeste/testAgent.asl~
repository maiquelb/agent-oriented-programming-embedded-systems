!start.


+!start : true <- .print("hello world.").

+currentDate(Date)[source(Sensor)]
   <- .print("Current date updated to ", Date, "by the sensor ", Sensor).
   
+currentTime(Time)[source(Sensor)]
   <- .print("Current time updated to ", Time, "by the sensor ", Sensor).

